.shared_media-header
  %h2= t('.title')

- content_for :right_column do
  = render 'comfy/admin/cms/sites/mirrors', :object => @shared_media

= comfy_form_for @shared_media, :as => :shared_media, :url => {:action => :update}, :html => {:multipart => true} do |form|
  = render form
